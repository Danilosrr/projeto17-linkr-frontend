import styled from "styled-components";

export default function AlertNewPosts(props){
    return (
        <Div>
            Component created
        </Div>
    );
}

const Div = styled.div`
    color: white;

`;